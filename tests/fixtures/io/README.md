# Mesh I/O fixtures

This directory keeps tiny DMPlex-class import fixtures.  Text formats are stored
as native files and binary HDF5-family formats have JSON manifests that document
the deterministic fixtures constructed by the HDF5-backed integration tests.
